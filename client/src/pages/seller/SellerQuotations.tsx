import SellerLayout from "@/components/seller/SellerLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { Eye, Plus } from "lucide-react";
import { Link } from "wouter";
const currency = (value: unknown) => Number(value ?? 0).toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
export default function SellerQuotations() { const { data, isLoading } = trpc.sellers.seller.quotations.useQuery({}); return <SellerLayout title="Meus orçamentos" description="Propostas emitidas sob sua responsabilidade comercial."><div className="mb-5 flex justify-end"><Button asChild className="bg-pink-600 hover:bg-pink-700"><Link href="/vendedor/orcamentos/novo"><Plus className="mr-2 h-4 w-4" />Novo orçamento</Link></Button></div><Card><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Orçamento</th><th className="p-4">Cliente</th><th className="p-4">Status</th><th className="p-4">Conversão</th><th className="p-4 text-right">Total</th><th className="p-4 text-right">Ações</th></tr></thead><tbody>{isLoading ? <tr><td colSpan={6} className="p-8 text-center text-slate-500">Carregando orçamentos...</td></tr> : data?.length ? data.map((quote: any) => <tr key={quote.id} className="border-t border-slate-100"><td className="p-4 font-medium">{quote.quotationNumber}</td><td className="p-4">{quote.clientName ?? "Cliente"}</td><td className="p-4 capitalize text-slate-600">{String(quote.status).replaceAll("_", " ")}</td><td className="p-4 text-slate-600">{quote.convertedOrderId ? `Pedido #${quote.convertedOrderId}` : "Não convertido"}</td><td className="p-4 text-right font-semibold">{currency(quote.total)}</td><td className="p-4 text-right"><Button asChild variant="outline" size="sm"><Link href={`/vendedor/orcamentos/${quote.id}`}><Eye className="mr-1.5 h-4 w-4" />Ver detalhes</Link></Button></td></tr>) : <tr><td colSpan={6} className="p-8 text-center text-slate-500">Você ainda não possui orçamentos.</td></tr>}</tbody></table></div></CardContent></Card></SellerLayout>; }
