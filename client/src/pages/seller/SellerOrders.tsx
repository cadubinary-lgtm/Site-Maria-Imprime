import SellerLayout from "@/components/seller/SellerLayout";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
const currency = (value: unknown) => Number(value ?? 0).toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
export default function SellerOrders() { const { data, isLoading } = trpc.sellers.seller.orders.useQuery({}); return <SellerLayout title="Meus pedidos" description="Apenas os pedidos vinculados à sua carteira comercial."><Card><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[660px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Pedido</th><th className="p-4">Cliente</th><th className="p-4">Pagamento</th><th className="p-4">Status</th><th className="p-4 text-right">Total</th></tr></thead><tbody>{isLoading ? <tr><td colSpan={5} className="p-8 text-center text-slate-500">Carregando pedidos...</td></tr> : data?.length ? data.map((order: any) => <tr key={order.id} className="border-t border-slate-100"><td className="p-4 font-medium">{order.orderNumber}</td><td className="p-4">{order.clientName ?? "Cliente"}</td><td className="p-4"><Badge variant={order.paymentStatus === "pago" ? "default" : "secondary"}>{order.paymentStatus === "pago" ? "Pago" : "Pendente"}</Badge></td><td className="p-4 text-slate-600">{String(order.status).replaceAll("_", " ")}</td><td className="p-4 text-right font-semibold">{currency(order.total)}</td></tr>) : <tr><td colSpan={5} className="p-8 text-center text-slate-500">Você ainda não possui pedidos na carteira.</td></tr>}</tbody></table></div></CardContent></Card></SellerLayout>; }
